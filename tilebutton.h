#ifndef TILEBUTTON_H
#define TILEBUTTON_H

#include <QObject>

class TileButton : public QObject
{
	Q_OBJECT
public:
	explicit TileButton(QObject *parent = 0);

signals:

public slots:
};

#endif // TILEBUTTON_H